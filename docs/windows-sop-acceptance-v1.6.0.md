# Windows 1.6.0 验证记录

2026-09-13，Windows 10 x64。

| 检查 | 结果 |
| --- | --- |
| 自动测试 | 117 项通过，23 个测试文件 |
| TypeScript 与构建 | 通过 |
| 截图预览保真回归、原生录像资源检查 | 通过 |
| Electron 开发构建与打包构建 SOP 回归 | 时间输入、进度条定位、当前帧插入、AI/文字/配图撤销与重做、步骤增删排序撤销、自动保存、历史恢复、重开、关闭保存通过 |
| 原有导出 | Word、PDF、HTML、Markdown 及长文 PDF 通过 |
| 用户问题录像只读复查 | FFprobe 与播放器均为 242.2 秒，可准确跳转到 100 秒；原接口返回 200 且无文件长度，修复后返回正确的 206 和 Content-Range |
| 飞书配置 | 界面保存与重开、默认 CLI 自动识别、现有用户真实只读连接通过 |
| 飞书图文发布 | 模拟 CLI 验证 XML 转义、正文/图片顺序、相对文件路径、用户身份、知识库 document_id、部分失败不重试；未创建真实云文档 |
| 安装版 | 安装后实际 EXE 启动与截图/素材库 E2E 通过；安装目录 app.asar 与通过 SOP 回归的打包文件一致 |
| 桌面入口和数据 | 快捷方式指向安装目录 1.6.0.0；更新前后 317 个设置、素材、录像与 SOP 数据文件哈希一致 |

本机证据保存在已忽略的 `windows/output`：

- `sop-ui-1789304239168/result.json`：开发构建界面验证。
- `sop-ui-1789304381350/result.json`：打包构建界面验证，含飞书真实登录测试。
- `source-seek-1789304616281.json`：原问题录像只读定位验证。
- `install-1.6.0/install-result.json`：安装、数据保留、快捷方式及应用包一致性。
- `packaged-video-1789304528269/result.json`：安装后的实际 EXE 验证。

安装包 `Ta-Windows-1.6.0-x64-Setup.exe` 的 SHA-256：

`DA1A03C244ADCADF51420317793B4E08ED6BDAA465AB58AA47F6DD617BA43B38`

飞书目标文件夹/知识库的写入权限、实际云端配图效果仍需首次真实发布验证。本次没有新增真实 AI 模型质量测试；AI 编辑的撤销回归使用确定性响应，1.5.0 的真实 CPA 短样本验证范围没有扩大。
