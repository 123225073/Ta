# Windows SOP 1.5.0 验收记录

日期：2026-09-13。

## 交付

- 安装包：`windows/release/Ta-Windows-1.5.0-x64-Setup.exe`。
- 文件版本：1.5.0。
- SHA256：`89FD1C1C342D8A57F477F6818E6DE42E044FA173E7A003021479314B87E251D1`。
- 已从 1.4.0 覆盖安装至 1.5.0，桌面“拓 Ta”快捷方式重新指向安装目录中的新版 EXE。覆盖安装前后 156 个设置、素材、录屏相关文件的 SHA256 一致；安装后的 app.asar 与已验证构建的哈希一致。证据：`windows/output/install-1.5.0/install-result.json`。

## 已通过

| 检查 | 结果及证据 |
| --- | --- |
| 单元测试 | 20 个测试文件、111 项测试通过。包含 SOP 修订冲突、历史恢复、时间和路径约束、真实裁切像素、AI 整体回滚、取消、外部图片来源保持、3 类 AI 服务单图及多图兼容。 |
| 类型、构建、预览保真 | `npm run dist` 中类型检查、构建、原截图预览保真和视频资源校验通过；NSIS 构建成功。 |
| SOP 开发版闭环 | `windows/output/sop-ui-1789229142177/result.json`。真实视频及音轨导入、转写请求、生成、对话查找和补图、人工插帧、保存、版本恢复、重新打开、关闭保存和四种导出通过。 |
| SOP 打包资源闭环 | `windows/output/sop-ui-1789230321674/result.json`。从 app.asar 加载控制器、SOP 服务、UI 和导出依赖；打包服务与本地编译文件逐字节一致，Mimik 许可已包含。 |
| 多页 PDF | 同一打包资源测试生成 `large-guide.pdf`，24 个含多张截图的步骤，37 页，288934 字节。使用本地临时 HTML，避免大文档依赖 data URL 长度。 |
| 旧录屏回归 | `windows/output/video-ui-1789229204380/result.json`。实际区域录制、音轨、暂停/恢复、项目恢复、剪辑、标记、放大后标记对齐、逐步笔迹、取消导出、关闭保存通过。原视频哈希不变，成片预期与实际均为 76 帧。 |
| 最终包启动 | `windows/output/packaged-startup-1789230362205/result.json`。实际 EXE 报告 1.5.0、packaged=true；6 个隔离测试快捷键注册成功。 |
| 实际 EXE 回归 | `windows/output/packaged-video-1789229699151/result.json`。实际 win-unpacked EXE 的截图、OCR、素材库和现有界面 E2E 通过，视频原生资源齐全。旧版仍运行占用快捷键，该次 EXE 测试不作为全局快捷键注册成功的证据。 |
| UI 检查 | 打包资源测试的夜幕、瓷白及 960px 宽窗口截图已生成；开发版截图已目视检查，短文本自适应高度。 |

## 覆盖安装后的实际 EXE 验证

已从安装目录运行实际 1.5.0 EXE，在独立测试配置目录完成截图、OCR、素材库、已有界面与原生视频资源检查。结果 ready=true，证据：windows/output/packaged-video-1789231060055/result.json。测试后从桌面快捷方式启动正常用户配置。

## AI 验收边界

自动化闭环使用本机确定性 HTTP 模型和语音响应，实际执行了视频解码、画面提取、多图 HTTP 请求、模型结果校验、文档修改和导出；没有把测试固定文案当作真实模型质量。

用户明确授权使用已配置 CPA 密钥后，已使用 gpt-5.6-terra 完成真实短样本验收。证据：`windows/output/sop-real-ai-1789230167971/result.json`、`generated.json`、`edited.json`、`guide.html`。对自制 6 秒视频生成 2 个步骤，模拟移除配图后，AI 查找并插入 3.5 秒处的真实画面，补充图注并重写说明；生成加对话耗时约 39 秒。CPA 配置文件哈希不变，密钥未输出到日志或文稿。人工检查修正了生成提示中对完成状态的约束，当前文稿不再把“已保存成功”写成点击保存的前提。该样本中的模型选择具体列表和选中状态未展示，文稿保留了对应待核对提示。

30 分钟 SOP 样本质量、复杂桌面应用的短暂菜单识别、真实中文语音准确率和长时间录屏稳定性未做完整验收。支持范围与人工补救方式见发布说明。
