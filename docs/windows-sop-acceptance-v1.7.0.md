# Windows 1.7.0 验收记录

日期：2026-09-14。测试项目、配图、CLI 安装目录和导出文件均使用隔离的 `windows/output/` 目录；用户原录像及 SOP 不作为可写测试数据。

## 已验证

- 25 个测试文件、124 项测试通过，包含时间线迁移、关联声音分割/移动/变速/删除、关键帧插值与变速、非法素材/标记拒绝，以及 CLI 发现、复用、安装校验失败处理。
- TypeScript 检查、正式构建、原截图预览保真回归、录屏工具和许可证检查通过。
- 原生录制探针通过：734×560、3.5 秒；工具窗口排除、暂停/恢复、分离音轨和分段 MP4 保持正常。
- Electron SOP 回归通过：真实本地 HTTP 模型模拟服务、AI 补图/修改整笔撤销、标题/步骤/图片撤销、视频定位与对应帧插入、版本恢复、关闭保存，Word/PDF/HTML/Markdown 导出。模拟模型不用于证明真实模型的文案质量。
- 多轨导出验证：6 秒素材分割变速后输出 4.5 秒；指定时间局部放大、叠加视频和限时标记通过逐帧像素检查，输出包含声音；补充素材经过真实转码、探测及注册。
- CLI 全新安装路径实测：在隔离目录下载、校验并安装官方 `lark-cli 1.0.95`。GitHub 公共 API 限流时，使用官方 npm 版本信息和官方发布校验文件；不替换机器已有 CLI。

## 证据文件

- `windows/output/sop-ui-1789351127897/result.json`
- `windows/output/timeline-render-1789351360604/result.json`
- `windows/output/cli-install-1789351127059/result.json`
- `windows/output/timeline-ui-1789351692294/result.json`
- `windows/output/timeline-ui-1789351967357/result.json`：最终安装包内代码；200% 查看与滚动、全屏、实际拖动、变速、关键帧、导入/导出及账号实际连接全部通过。

界面自动化覆盖大窗口、全屏、片段点击选择、分割、时间编辑、实际鼠标拖动与右边缘变速、原声音同步、区域框选、关键帧时间修改、导入音频、Ctrl+Z/重做、MP4 导出及重新打开。

## 验证边界

飞书现有账号已进行实际只读连接检测；安装包验证与实际连接不代表用户目标云文档发布已验收。本次没有创建云端测试文档，也没有代替用户在官方页面完成新应用/新账号授权。新应用配置及登录页面已实现二维码与链接流程，需要用户完成网页授权。

本版聚焦教学视频多轨剪辑、恒定倍速、画中画、限时标记和画面缩放关键帧；不声称实现完整剪映或物体自动跟踪。删除/移动片段留下的空白时间不会自动吸合。

## 本机安装

- 安装程序：`windows/release/Ta-Windows-1.7.0-x64-Setup.exe`，SHA-256：`3298DEA4E523AC68C947591434D716309E0F24EC4C1024D7F488AA8898D5FC3C`。
- 实际安装程序版本 `1.7.0.0`；已安装 `app.asar` 与通过测试的安装包内容哈希一致。
- 桌面「拓 Ta」快捷方式已指向更新后的程序。
- 321 个原设置、录像、声音、截图、SOP 等数据文件在升级前后哈希一致。
- 记录：`windows/output/install-1.7.0/install-result.json`。
- 已安装程序的实际启动、截图/素材库交互与录屏资源检查通过：`windows/output/packaged-video-1789352123807/result.json`。
