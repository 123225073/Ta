# Ta Windows v1.1.2

发布日期：2026-08-27

## 修复

- 首次按住左键拖动并松开后，选区立即进入可编辑状态，不再要求先按 `Enter`。
- 在选区内部拖动会移动原选区，不再误创建新框。
- 增加四边和四角共 8 个缩放控制点，并限制选区始终位于当前显示器内。
- 双击选区或点击“完成”即可提交截图；`Enter` 保留为可选快捷键，`Esc` 与右键仍可取消。

## 验证

- 新增选区状态机回归与极端边界测试。
- Electron 端到端烟测实际注入鼠标事件，验证创建、松开保留、移动、缩放和 8 个控制点。
